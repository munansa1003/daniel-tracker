# 애플워치 운동 자동 수신 — 검증 기록 (Definition of Done 마감)

기준 문서: `docs/prompts/health-import-api-prompt.md` · 조립 가이드: `docs/shortcut-recipe.md`
검증일: **2026-08-05** · 대상: 프로덕션 `https://daniel-tracker.vercel.app`

## 배포 이력

| 단계 | 내용 | 커밋/PR |
|---|---|---|
| 구현 (1안 사서함) | 검문소 API + 앱 병합 + 계약 테스트 10+α | PR #75 → `51c3a2b` |
| Phase 5 문서 최종화 | 가이드에 실제 배포 URL 반영 | PR #76 → `576f77b` |
| 환경변수 3종 | `IMPORT_TOKEN` · `IMPORT_UID` · `IMPORT_CUTOVER_DATE=2026-08-06` — Production 스코프, Redeploy 완료 | 2026-08-05 |

## 자동 검증 (게이트)

- `eslint src api` 클린 · vitest **30파일 268테스트** 전부 통과 (계약 테스트 10+α + 기존 골든셋 회귀 포함)
- CI `quality-gate`: PR·main 머지 커밋 모두 success

## curl 실전 시뮬레이션 (프로덕션, 2026-08-05)

환경변수 반영 확인 리트머스: 토큰 없이 POST → 설정 전 `503`(잠금) → 설정·재배포 후 `401`(문 열림) 전환 확인.

| # | 케이스 | 결과 | 판정 |
|---|---|---|---|
| 1 | 정상 유산소 1건 (Outdoor Run, 8/6, 446kcal) | `200 → "1건 추가"` | ✅ 검문 통과·사서함 적재 |
| 2 | 동일 페이로드 재전송 | `200 → "0건 추가 · 중복 1 무시"` | ✅ insert-only |
| 3 | 근력 (Functional Strength Training) | `200 → "0건 추가 · 근력 1 제외"` | ✅ 화이트리스트 (검증 당시 정책 — 2026-08-06부터는 '근력 운동'으로 수입) |
| 4 | 잘못된 토큰 | `HTTP 401` | ✅ 인증 |

## 앱 관통 확인 (검문소 → 사서함 → 앱 병합 → 목표 반영)

- 8/6 기록에 `18시 러닝 ⌚ · 30분 · -446kcal` 병합 표시 (출처 배지 정상)
- 유효목표 자동 상향: 1,528 → **1,751** (+223 = 446×50% 되먹기 — 기존 단일 출처 계산 그대로)
- 설정 → 데이터 → "자동 가져오기(애플워치)" 카드: 켜짐 · 컷오버 2026-08-06 · 수신 로그 3건 (`+1 중복0` / `+0 중복1` / `+0 제외1`)

## 남은 일 (사용자 수행)

- [x] curl 시험 기록(8/6 러닝 ⌚ 446kcal) 앱에서 삭제 — 길게 눌러 → 삭제. 접수 도장이 남아 재전송으로 되살아나지 않음
- [x] ~~폰 단축어 조립~~ → 네이티브 단축어는 이 기기에서 불가로 판명(운동 읽기 동작 없음). **HAE 단축어 동작 경로로 대체 완성** — `docs/hae-setup.md`
- [x] **컷오버(2026-08-06)부터 화이트리스트 종목의 수동 입력 중단** (수동+자동 이중 계상 방지 — dedup은 자동 간 중복만 차단). 근력도 자동 수입되므로 개별 종목 수동 입력 중단 — 세부는 '근력 운동' 기록의 메모에 기입

## 최종 구성 확정 (2026-08-06 밤 — 추가 검증)

- HAE **Basic ₩4,400 1회성 구매** 후 순수 Basic 상태에서 단축어 경로 작동 재확인
- **실전 관통 검증**: 워치 운동 종료 → 자동화 트리거 → 단축어 → 서버 → 앱 반영 (23시 시험 운동, 종료 후 ~1분 내 표시)
- 앱 내 REST 자동화(프리미엄)는 폐기 — 최종 아키텍처·레시피·운영 수칙은 `docs/hae-setup.md`가 단일 출처
- 화이트리스트 최종: 유산소 7종 + 스트레칭(워치 '식히기', PR #84·#86) + 근력 운동(PR #83) · 본문 인코딩 보강(PR #85)

## 이후 확장 후보 (별도 결정 필요 — 이번 범위 아님)

- ~~근력 2단계: 워치 덩어리 수신 → "쪼개기(배분)" 플로우로 세부 운동 변환~~ → 2026-08-06 더 단순한 안으로 확정: '근력 운동' 한 건 자동 수입 + 세부 종목은 메모 수동 기입
- HAE 등 다른 브릿지 폴백: `api/_lib/import-rules.js` 재사용, 페이로드 파서만 추가
